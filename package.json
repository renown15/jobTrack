{
  "scripts": {
    "frontend:install": "cd frontend && npm ci",
    "frontend:build": "cd frontend && npm run build",
    "frontend:serve": "npx serve -s frontend/dist -l 3000",
    "mypy": "poetry run mypy . || true"
  },
  "devDependencies": {
    "jsdom": "^27.2.0",
    "c8": "^10.1.3",
    "serve": "^14.1.2"
  }
}