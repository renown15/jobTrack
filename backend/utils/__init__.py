"""Utilities package for JobTrack.

This file exists to make `utils` an explicit package so static tools
and type checkers map module names consistently (avoids duplicate-name
errors during `mypy` runs).
"""
